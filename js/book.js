export default class Book{
    constructor(autori=["not given"],year="not given",language=["not found"],title="not given"){
        this.autori=autori;
        this.year=year;
        this.language=language;
        this.title=title;
    }
}